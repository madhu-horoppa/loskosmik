package com.ps.los;

import java.lang.Number;

rule "CreditScore"
	dialect "mvel"
	ruleflow-group "CreditScoreCal"
	when
		Customer( age < 18 )
	then
		Customer fact0 = new Customer();
		fact0.setCreditscore( 5 );
		insert( fact0 );
end
