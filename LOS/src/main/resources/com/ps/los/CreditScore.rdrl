package com.ps.los;

import java.lang.Number;

rule "CreditScore"
	dialect "mvel"
	ruleflow-group "CreditScoreCal"
	when
		cu : Customer( age < 18 )
	then
		modify( cu ) {
				setCreditscore( 5 )
		}
end
