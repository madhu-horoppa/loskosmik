package com.ps.los;

import java.lang.Number;

rule "CreditScoreCal"
	dialect "mvel"
	ruleflow-group "CreditScoreCal"
	when
		customer1 : Customer( age < 18 )
		Customer( age > 18 && age <= 50 )
	then
		customer1.setCreditscore( 5 );
end
