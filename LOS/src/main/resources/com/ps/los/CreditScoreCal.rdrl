package com.ps.los;

import java.lang.Number;

rule "CreditScoreCal"
	dialect "mvel"
	ruleflow-group "CreditScoreCal"
	when
		customer : Customer( age < 18 )
		customer1 : Customer( age > 18 && age < 50 )
	then
		customer.setCreditscore( 5 );
		customer1.setCreditscore( 10 );
end
